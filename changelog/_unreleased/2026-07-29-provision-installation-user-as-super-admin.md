---
title: Provision the installation account as a super administrator
---
# Core
* Kept `user:create` administrator provisioning unchanged and changed the default installation username to `supperadmin` for both the web installer and `system:install --basic-setup`.
